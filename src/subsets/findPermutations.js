// @ts-check

/**
 * 
 * @param {number[]} nums
 * @returns {number[][]}
 */
// export default function findPermutations(nums) {
//   const permutations = [];

//   for (let i = 0; i < nums.length; i++) {
//     const curr = nums[i];
//     const subset = [[]];
//     const subsetLen = subset.length;
//     for (let j = 0; j < subsetLen; j++) {
//       const perm = [...subset[j]];
//       perm.push(curr);
//     }
//   }

//   return permutations;
// }
